import {FabricHub} from './_setup.jsy'

import tcp from '../dist/plugin-tcp'
import tls from '../dist/plugin-tls'
import direct from '../dist/plugin-direct'

import router from '../dist/plugin-router-node'

const Hub = FabricHub.plugin @
  router()
  tcp(), tls(), direct()


const h1 = new Hub()
const h2 = Hub.create()
