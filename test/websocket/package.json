{
  "private": true,
  "dependencies": {
    "faye-websocket": "^0.11.1",
    "ws": "^6.1.0"
  },
  "devDependencies": {
    "rollup": "^0.66.6"
  },
  "scripts": {
    "clean": "rm -rf ./cjs/* ./umd/*",
    "build": "rollup --config",
    "watch": "npm -s run build -- --watch",
    "pretest": "npm -s run build",
    "test": "true"
  }
}
